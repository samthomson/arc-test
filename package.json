{
  "name": "arctest",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "create": "AWS_PROFILE=lambda-play AWS_REGION=us-west-1 arc-create",
    "deploy": "AWS_PROFILE=lambda-play AWS_REGION=us-west-1 arc-deploy",
    "start": "AWS_PROFILE=lambda-play AWS_REGION=us-west-1 NODE_ENV=testing arc-sandbox"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@architect/workflows": "^1.0.15"
  }
}
